#include <stdio.h>

int main() {

    // A for loop repeats a section of a code a limited amount of times

	for (int i = 0; i <= 10; i++) {
 		printf("%d\n", i);
    }

    // This loop will count to ten
}