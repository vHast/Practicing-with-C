#include <stdio.h>
#include <math.h>

int main() {
    double A = sqrt(9); // Square root of 9, 3.000000
    double B = pow(2, 4); // Power
    int C = round(3.14); // Rounding number
    int D = ceil(3.14); // Always round into the upper number, 4
    int E = floor(3.99); // Always round int othe lower number, 3
    double F = fabs(-100); // How far a number is away from 0
    double G = log(3); // Calculates the logarithm of a numberl
    double H = sin(45);
    double I = cos(45);
    double J = tan(45); // Tangent

    printf("\n%1f", G);
}